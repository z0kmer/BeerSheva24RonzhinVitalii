export function myMap(callback)  {
    //TODO
    //convert array of elements to another array by applying callback function for each element
    //callback function is a function that takes three paraeters: current element,
    //  index of current element, reference to array itself 
}
export function myReduce(callback) {
    //TODO
    //see documentation
}